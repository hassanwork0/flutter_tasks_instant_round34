 
    import '../repositories/repositories.dart';

    class GetLogin_or_signupUseCase {
        final Login_or_signupRepository repository;
      
        GetLogin_or_signupUseCase({required this.repository});
      
        // Future<User> execute(String userId) async {
        //   return userRepository.getUser(userId);
        // }
      }
      